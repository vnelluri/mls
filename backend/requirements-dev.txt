# Local development only -- never installed in the prod/CI image.
# moto[server] provides a pure-Python local DynamoDB emulator (no Docker/JVM);
# the test suite uses moto's in-process mock_aws instead of the server.
moto[server]>=5.0,<6.0
pytest>=8.0,<9.0
