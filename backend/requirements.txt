fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
boto3>=1.34,<2.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-jose[cryptography]>=3.3,<4.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
# Real executors (SNOWFLAKE_MODE=real / DQ_MODE=real). Imported lazily -- the
# mock paths never touch them -- but installed in every image so flipping a
# deployment to real mode is a config change, not a rebuild.
snowflake-connector-python>=3.12,<4.0
pyarrow>=15.0,<25.0
